apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: provisioner-ingress
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: provisioner
                port:
                  number: 80
